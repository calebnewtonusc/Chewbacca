# 1000 things: status

**193 of 1000 fixed.** Generated by `tools/fixed.py` from the commit
log, not written by hand. A commit closes an item by naming it in its message,
so every line below points at the commit that has to justify it.

Open items are not a backlog anyone promised to finish. Many are deliberate
(see [ROADMAP.md](ROADMAP.md)), and the last category is unreasonable on purpose.

| Section | Fixed | Total |
| --- | --- | --- |
| Install and first run | 2 | 25 |
| setup.sh itself | 0 | 25 |
| doctor.sh | 14 | 25 |
| Uninstall and reversibility | 6 | 20 |
| CLAUDE.md and the context tax | 11 | 25 |
| Skills: what is missing | 0 | 25 |
| Skills: quality and triggering | 7 | 25 |
| Evals and testing | 18 | 25 |
| Slash commands | 4 | 25 |
| Hooks | 15 | 25 |
| The chewbacca CLI | 23 | 25 |
| The people CLI | 0 | 25 |
| The coursework CLI | 0 | 25 |
| Second brain integration | 0 | 25 |
| Memory | 2 | 25 |
| Mac control | 0 | 25 |
| Messages and iMessage | 0 | 25 |
| Email | 2 | 25 |
| Calendar | 0 | 25 |
| Plynn and voice | 0 | 25 |
| MCP servers | 0 | 25 |
| Security | 7 | 25 |
| Privacy | 6 | 25 |
| Permissions | 2 | 25 |
| Performance | 7 | 25 |
| Observability | 9 | 25 |
| Multiple machines | 1 | 25 |
| Portability | 5 | 25 |
| Documentation | 17 | 25 |
| README and positioning | 8 | 25 |
| Onboarding a non-technical user | 2 | 25 |
| Distribution and growth | 0 | 25 |
| Community and contribution | 0 | 25 |
| Versioning and releases | 25 | 25 |
| Kits | 0 | 25 |
| School and study | 0 | 25 |
| Life | 0 | 25 |
| Writing | 0 | 25 |
| Engineering workflow | 0 | 25 |
| Moonshots | 0 | 30 |

## Fixed

- **7.** Nothing writes an install manifest, so `uninstall.sh` is guessing at what setup actually did.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **13.** There is no `--minimal` profile for someone who wants the standards and none of the Mac control.  
  `027e8f0` portable: the half of this kit that is not macOS, on any machine
- **52.** Every check is pass/warn/fail with no severity ranking, so a missing PATH entry looks like a missing MCP server.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **53.** `doctor` has no `--fix` mode. It names problems and hands them back.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **54.** No JSON output, so nothing else can consume the result.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **55.** No exit code that distinguishes "warnings only" from "broken".  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **56.** Checks are hardcoded in one 28KB script instead of being a directory of small check files.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **59.** It reports "no dangling skill links" but never verifies a skill loads.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **61.** No historical record, so you cannot see that a check started failing three days ago.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **62.** `PUBLIC_CONTEXT_DIR not set` is a warning on a machine where it will never be set. Warnings that never clear get ignored.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **66.** No check that hooks are executable and non-empty, beyond existence.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **67.** Nothing catches a hook that is silently erroring on every session start.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **68.** Nothing times the hooks, so a slow hook taxing every session is invisible.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **70.** Doctor never checks its own version against the repo.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **72.** Checks cannot be muted per machine.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **73.** Nothing verifies Full Disk Access, which is the permission most likely to be silently missing.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **76.** Uninstall does not know what setup installed, because setup never recorded it.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **81.** The people database survives uninstall with no prompt about deleting personal data.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **82.** No export path before uninstall, so leaving means losing the accumulated context.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **88.** Uninstall is not tested in CI.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **89.** Nothing verifies the machine is actually clean after uninstall.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **91.** Reinstalling after an uninstall does not restore prior state.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **96.** `CLAUDE.md` is 957 lines and 39KB, and it loads into every single session before the user types anything.  
  `bb99df5` context: stop charging every session for the frontend rules
- **97.** Nobody has measured what that costs per session in tokens or in dollars.  
  `bb99df5` context: stop charging every session for the frontend rules
- **98.** It mixes UI design mandates, tech stack opinions, debugging protocol, and memory rules in one file, so a backend session pays for the animation rules.  
  `bb99df5` context: stop charging every session for the frontend rules
- **99.** There is no conditional loading. The Framer Motion section loads during a shell script session.  
  `bb99df5` context: stop charging every session for the frontend rules
- **103.** `MEMORY.md` already blew past its own size limit (27.9KB against 24.4KB) and the fix was a warning, not a design change.  
  `bb99df5` context: stop charging every session for the frontend rules
- **104.** There is no tiered context: hot, warm, cold.  
  `bb99df5` context: stop charging every session for the frontend rules
- **105.** The always-on rules directory has 8 files but doctor reports 4 installed. Three numbers exist for the same thing.  
  `a044bc8` counts: one number for one thing, checked in CI
- **107.** No per-project override that turns off half the global file.  
  `bb99df5` context: stop charging every session for the frontend rules
- **108.** The design system rules assume Next.js and React, and load for a Swift project.  
  `bb99df5` context: stop charging every session for the frontend rules
- **115.** No compiled form. It could be generated from the rules directory and is not.  
  `bb99df5` context: stop charging every session for the frontend rules
- **120.** It has no table of contents.  
  `bb99df5` context: stop charging every session for the frontend rules
- **146.** 12 of 21 first-party skills have no evals at all, and every one that does is a `mac-*` skill.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **152.** No skill declares its dependencies on CLI tools in a machine-readable way.  
  `1f0d7df` skills: declare the tools they promise, and doctor reads the declaration
- **163.** Skill evals are prompts and expected behavior, with no automated scoring.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **164.** Nothing runs the evals on a schedule.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **165.** Nothing runs the evals before a merge.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **167.** There is no per-skill usage counter, so nobody knows which of the 79 installed skills ever fires.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **169.** Skills are all text, so a skill cannot ship a small deterministic tool alongside it without a separate bin entry.  
  `1f0d7df` skills: declare the tools they promise, and doctor reads the declaration
- **171.** There is no test suite for `bin/people`, which manages 1,165 records of real relationships.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **172.** There is no test suite for `bin/coursework`, which is the source of truth for graded deadlines.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **173.** CI compiles Python and checks bash syntax. Neither proves any behavior is correct.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **174.** No integration test runs the installer end to end on a clean macOS runner.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **175.** GitHub Actions has no macOS job at all, for a macOS-only kit.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **176.** Shellcheck runs at `-S error`, so every warning-level finding is invisible.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **177.** There is no golden-output test for `doctor`.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **178.** No fixture machine state to test doctor's failure branches, so only the all-pass path is ever exercised.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **179.** Nothing tests the hooks.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **180.** Nothing tests the statusline.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **181.** No test asserts that the slop checker's own docs pass the slop checker.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **183.** No fuzzing of the people database importer against malformed input.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **185.** No test for a Mac with permissions denied, which is the most common real state.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **186.** Evals live in nine directories with no runner that executes all of them.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **187.** No regression corpus of prompts that previously failed.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **188.** No latency budget for any operation.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **189.** No test that the kit works with a fresh `~/.claude` and no prior state.  
  `18cb370` tests: 69 of them, on a macOS runner, and they found five real bugs
- **194.** No CI check that README counts match reality, which is why the README says 42 skills, the tree has 21, and doctor reports 79.  
  `a044bc8` counts: one number for one thing, checked in CI
- **198.** There is no `/help` that lists them by what you are trying to do.  
  `f725d44` commands: findable, and two that argued with the standards
- **204.** `/cleanup` tells the agent to leave TODOs for the user to decide, which contradicts the do-it-yourself rule.  
  `f725d44` commands: findable, and two that argued with the standards
- **211.** `/audit` greps for TODO in TypeScript and Python only, which misses the shell that this repo is actually written in.  
  `f725d44` commands: findable, and two that argued with the standards
- **212.** Commands cannot be shared or exported individually.  
  `f725d44` commands: findable, and two that argued with the standards
- **221.** Eight hooks run on session start, tool use, and stop, and none of them are timed.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **222.** A hook that fails silently degrades every session and nothing surfaces it.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **223.** `session-context.sh` is 7.8KB of bash running before every session and its output is unbounded.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **224.** Hook output is injected as context with no size cap, so a bad day of coursework data can eat the window.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **225.** There is no hook debugger.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **226.** Hooks cannot be disabled individually without editing JSON.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **227.** No hook has a timeout.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **228.** The Stop hook regenerates the extension inventory, so quitting a session does filesystem work the user did not ask for.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **229.** `format-and-sync.sh` runs on every Write and Edit, which means a formatter runs on files the user is mid-thought on.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **230.** Nothing logs what hooks did.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **231.** Hooks cannot be tested locally without starting a session.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **232.** There is no hook that catches a session ending with uncommitted work.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **235.** No hook warns when context is nearly full.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **239.** No hook validates that the tools a skill promised are present before the skill runs.  
  `1f0d7df` skills: declare the tools they promise, and doctor reads the declaration
- **242.** Hooks write to stdout as the interface, which makes error handling ambiguous.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **246.** Seven verbs: doctor, setup, update, add-skill, install, uninstall, where. That is an installer, not a tool.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **247.** `chewbacca people`, `chewbacca due`, `chewbacca brief` do not exist, though all three have backing tools.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **248.** No `chewbacca status` that says what is on and what is off.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **249.** No `chewbacca skills` to list, search, enable, or disable skills.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **250.** No `chewbacca log` to show what the kit has done recently.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **251.** No shell completion for any shell.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **252.** No `--json` on any subcommand.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **253.** `chewbacca update` pulls and re-runs setup with no changelog shown.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **254.** No `chewbacca changelog`.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **255.** No version command, because there are no versions.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **256.** No `chewbacca config` to read or set anything.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **257.** There is no man page.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **258.** `chewbacca --help` is a heredoc, not generated from the subcommand table.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **259.** No subcommand aliases.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **260.** Errors do not suggest the next command to run.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **261.** No `chewbacca try <skill>` to exercise one skill without a full session.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **262.** Nothing exposes the people, coursework, and second-brain tools under one roof, so the user learns four CLIs.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **263.** No plugin mechanism for third-party subcommands.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **264.** No `chewbacca export` for moving to a new machine.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **265.** No `chewbacca import`.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **266.** No interactive mode.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **267.** Nothing prints the kit's own footprint on the machine.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **268.** No `chewbacca why <behavior>` that traces which rule or skill caused something.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **346.** `MEMORY.md` exceeded its own size limit and the system's answer was a warning in a system reminder.  
  `bb99df5` context: stop charging every session for the frontend rules
- **347.** Index entries are one line each and the file still overflows, which means the design does not scale past a few hundred facts.  
  `bb99df5` context: stop charging every session for the frontend rules
- **434.** Prompt injection through email content is not defended against anywhere.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **435.** No allowlist for senders whose instructions may be acted on.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **523.** `Bash(rm -rf ~)` is denied but `Bash(rm -rf $HOME)` is not.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **524.** Denials are string patterns, so any equivalent phrasing bypasses them.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **527.** Prompt injection through a read file, a web page, or an email is undefended.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **531.** No dependency pinning for the Node dev dependencies.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **534.** No SECURITY.md threat model, only a disclosure address.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **544.** No kill switch for a compromised machine.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **545.** No review of what the kit has permission to do, in one place, in plain language.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **546.** The kit reads texts, email, contacts, calendar, and screen, and there is no single page saying what leaves the machine.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **547.** No data inventory.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **550.** Nothing distinguishes data that may be sent to an API from data that must stay local.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **562.** Nothing surfaces third-party data flows through MCP connectors.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **568.** Nothing documents where each cache lives on disk.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **570.** Privacy is described as "local" and never proven.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **573.** No deep link into the right System Settings pane per permission.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **581.** Full Disk Access is required for messages and is not checked by doctor.  
  `df583b3` doctor: severity, JSON, --fix, history, and four checks that can fail
- **597.** Session start runs eight hooks before the user can type.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **598.** The session-context hook does filesystem and git work on every start.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **599.** No caching of session context between sessions minutes apart.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **603.** The Stop hook regenerates inventory on every session end.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **611.** Nothing measures cold versus warm start.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **612.** No budget for how much context the kit may consume before the user's own work starts.  
  `bb99df5` context: stop charging every session for the frontend rules
- **617.** Nothing truncates a large tool result before it enters context.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **621.** Nobody can answer "what did the kit actually do today".  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **622.** No structured log anywhere.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **623.** No session transcript archive under the kit's control.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **624.** Nothing records which skills loaded per session.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **625.** Nothing records which rules were in context.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **626.** No error aggregation.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **627.** Failures are printed and lost.  
  `93b6c5e` hooks: time themselves, log themselves, and stop blocking the session
- **633.** No weekly report on the kit's own behavior.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **645.** The kit cannot explain its own decisions after the fact.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **664.** Nothing handles a migrated user account with a new username.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **671.** macOS only, stated proudly, which caps the audience at a fraction of developers.  
  `027e8f0` portable: the half of this kit that is not macOS, on any machine
- **672.** Nothing degrades gracefully on Linux, it simply does not run.  
  `027e8f0` portable: the half of this kit that is not macOS, on any machine
- **675.** No way to install only the platform-neutral half.  
  `027e8f0` portable: the half of this kit that is not macOS, on any machine
- **676.** Everything assumes Claude Code specifically.  
  `027e8f0` portable: the half of this kit that is not macOS, on any machine
- **679.** No export to a generic AGENTS.md.  
  `027e8f0` portable: the half of this kit that is not macOS, on any machine
- **696.** `docs/REFERENCE.md` is 1,028 lines, longer than the README by six times, and nothing routes a reader between them.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **697.** Nineteen docs files with no index page.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **698.** The README claims 42 skills, the tree has 21, and doctor reports 79 installed. Three numbers, one concept.  
  `a044bc8` counts: one number for one thing, checked in CI
- **699.** The README claims 7 always-on standards while the rules directory holds 8.  
  `a044bc8` counts: one number for one thing, checked in CI
- **700.** Generated badge counts and prose counts are maintained separately.  
  `a044bc8` counts: one number for one thing, checked in CI
- **705.** No quickstart that fits on one screen.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **706.** No troubleshooting page organized by symptom.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **707.** No FAQ.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **708.** No glossary, despite the kit inventing terms like kit, layer, and standard.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **712.** No page on what the kit deliberately does not do.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **713.** No architecture diagram.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **714.** No data flow diagram, which is what a privacy-conscious reader needs.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **715.** Docs are not versioned with the code.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **717.** No changelog.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **718.** No release notes.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **719.** No roadmap.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **720.** Nothing tells a contributor where to start.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **723.** The value table is strong and buried below install instructions.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **728.** No section on cost, which is the first question after "what is it".  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **729.** Nothing sets expectations about what will break.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **730.** No "is this for me" filter near the top.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **741.** Nothing tells the reader how long install takes before they start.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **742.** No uninstall instructions in the README.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **743.** No "what it does to your machine" summary above the fold.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **744.** Nothing addresses the security objection that curl-pipe-bash raises immediately.  
  `2e3a878` security: a threat model, a data inventory, and a deny list that means it
- **752.** Nothing explains what a skill is either.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **753.** There is no first-session tutorial.  
  `a01e5ba` docs: an index, a troubleshooting page, and five things that did not exist
- **821.** There are no versions. Not one tag in the repo.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **822.** No semantic versioning of the standards, so "which rules" is unanswerable.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **823.** `chewbacca update` pulls main and re-runs setup with no diff shown.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **824.** No way to pin to a known-good commit.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **825.** No way to roll back an update.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **826.** No changelog generated from commits.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **827.** No release notes for a user who does not read diffs.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **828.** Nothing warns before a breaking change to a hook contract.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **829.** No deprecation window for anything.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **830.** No migration scripts between versions.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **831.** Nothing records which version is installed on a machine.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **832.** Doctor cannot compare installed version to latest.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **833.** No update notification.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **834.** No auto-update option, and no explicit statement that there is not one.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **835.** Nothing separates a config change from a code change during update.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **836.** User customizations are overwritten by update with no merge.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **837.** The session opener is customizable and update clobbers it.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **838.** No local override layer that survives updates.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **839.** Nothing backs up before an update.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **840.** No staged rollout of a risky change.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **841.** No beta channel.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **842.** Nothing tests an upgrade from an old install to the current one.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **843.** No LTS notion for people who want it to stop changing.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **844.** Commit messages are excellent and are the only release documentation.  
  `713e19e` cli: nine new verbs, a version, and a changelog
- **845.** Nothing announces anything anywhere.  
  `713e19e` cli: nine new verbs, a version, and a changelog
