#!/usr/bin/env bash
# stays: places to stay in a city, as a table with links and a recommendation.
# The procedure lives in procedures/stays-compare; this is its name on PATH.
exec python3 "$(cd "$(dirname "$(readlink "$0" || echo "$0")")/.." && pwd)/procedures/stays-compare/run.py" "$@"
