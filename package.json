{
  "name": "chewbacca",
  "private": true,
  "description": "Turns Claude into the world's best sidekick. Nothing to remember, nothing to type.",
  "scripts": {
    "format": "prettier --write '**/*.{ts,tsx,js,jsx,json,css,md}' --ignore-path .gitignore",
    "format:check": "prettier --check '**/*.{ts,tsx,js,jsx,json,css,md}' --ignore-path .gitignore"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11"
  }
}
